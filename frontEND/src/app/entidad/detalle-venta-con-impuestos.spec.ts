import { DetalleVentaConImpuestos } from './detalle-venta-con-impuestos';

describe('DetalleVentaConImpuestos', () => {
  it('should create an instance', () => {
    expect(new DetalleVentaConImpuestos()).toBeTruthy();
  });
});
